# app/rag/ingest.py
"""
로컬에 있는 마크다운(.md)이나 PDF 문서를 읽어서
Pinecone 벡터 데이터베이스에 주입(Ingestion)하는 1회성 스크립트입니다.

현재는 Google Colab에서 전처리 및 업로드를 완료했기 때문에
서버 실행 시에는 이 코드가 동작하지 않아도 됩니다. (보관용/기록용)
"""


def run_ingestion():
    # 추후 지식 베이스(가이드라인)가 업데이트 될 때,
    # 서버 관리자가 터미널에서 수동으로 실행할 로직이 들어갑니다.
    print("Pinecone 데이터 업데이트가 필요할 때 이 스크립트를 구현하세요.")
